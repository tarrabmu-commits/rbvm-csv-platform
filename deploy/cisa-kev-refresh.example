# Source-only systemd refresh derives CVEs from the current canonical Cases API.
RBVM_API_BASE_URL=http://127.0.0.1:8080

# The supplied user service is sandboxed for the default per-user work/output paths.
# Leave RBVM_INTELLIGENCE_WORK_DIR and RBVM_KEV_OUTPUT_DIR unset unless you also update
# the service ReadWritePaths for your chosen locations.
RBVM_KEV_KEEP=14

# Legacy static RBVM_KEV_INPUT=/path/to/cves.csv is intentionally not used by this service.
# The source-specific scheduled script still accepts RBVM_KEV_INPUT for controlled/manual runs.

# Optional only for a hardened/authenticated API deployment. The source-specific key wins;
# otherwise RBVM_INTELLIGENCE_API_KEY can be shared with the canonical CVE read.
# RBVM_INTELLIGENCE_API_KEY=from-a-secret-manager
# RBVM_KEV_API_KEY=optional-source-specific-override

# Optional bound for the generated evidence handoff (default 32 MiB).
RBVM_KEV_MAX_BYTES=33554432

# Optional controlled replay/testing input. Leave unset in normal production operation.
# When configured, the file is parsed through the same source validation logic and the semantic
# source remains the pinned official CISA KEV feed.
# RBVM_KEV_OFFLINE_INPUT=/absolute/path/to/known_exploited_vulnerabilities.json
