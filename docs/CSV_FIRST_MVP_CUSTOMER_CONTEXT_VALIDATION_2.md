# CSV-First MVP Customer Context V2 Validation Marker

Run the full pull-request verification suite for the simplified customer workflow:

- one uploaded vulnerability CSV defines run scope;
- public vulnerability intelligence remains automatic;
- the Assets setup UI requests only Asset Criticality and Internet Facing;
- reusable customer context uses RBVM_CUSTOMER_ASSET_BUNDLE_V2;
- no browser persistence or managed-asset database dependency is introduced into the MVP handoff.
