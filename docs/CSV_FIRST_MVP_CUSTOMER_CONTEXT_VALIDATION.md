# CSV-First MVP Customer Context Validation

Validation target: `CSV_FIRST_CUSTOMER_ASSET_SETUP_UI_V2` + `RBVM_CUSTOMER_ASSET_BUNDLE_V2`.

The full repository verification suite must confirm that the current CSV-first UI requests only Asset Criticality and Internet Facing from the customer, preserves the public-intelligence boundary, builds reproducibly, passes PostgreSQL integration, and passes CodeQL.
