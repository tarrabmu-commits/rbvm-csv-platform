# CSV-First Customer Flow Validation

Validation target: `CSV_FIRST_CUSTOMER_ASSET_SETUP_UI_V1` + `CSV_FIRST_PUBLIC_INTELLIGENCE_HTTP_V1` + `RBVM_CUSTOMER_ASSET_BUNDLE_V1`.

The verification pipeline must compile the CSV-first enabled launcher, compose the customer-flow frontend bundle, preserve existing platform tests, build the reproducible distribution, run PostgreSQL integration, and pass CodeQL.
