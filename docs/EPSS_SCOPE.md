# EPSS Contract Scope

This increment ends at validated `EPSS_CSV_V1` evidence. PostgreSQL persistence,
transactional import, runtime API/UI, scheduling, freshness policy, thresholds, and
RBVM decision logic are separate later increments.
