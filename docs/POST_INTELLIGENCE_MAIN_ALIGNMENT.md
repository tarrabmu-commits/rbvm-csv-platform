# Post-Intelligence Main Alignment

This maintenance increment aligns repository-facing documentation and hygiene after the canonical dedicated-intelligence read/refresh work landed on `main`.

It does not define or calculate Risk, Priority, Treatment, or SLA.

## Current architecture reflected by this pass

- PostgreSQL migrations extend through V22.
- Decision Input V3 is the current immutable Formula boundary.
- Managed Asset context is bound through the exact scanner↔managed-asset link event.
- Reachability and Business Impact are admitted to a Finding only through exact customer-confirmed Finding-context association events.
- Canonical Cases produce one normalized/deduplicated CVE inventory for NVD CVSS v3.1, FIRST EPSS, and CISA KEV refreshes.
- Finding/Case reads and intelligence summaries use dedicated CVSS/EPSS/KEV evidence stores.
- Missing evidence remains unknown/missing and multiple admissible sources remain ambiguous; no hidden source precedence is introduced.
- Formula readiness decisions and the Stage 8 golden-case corpus are already frozen. The next methodology stage is the versioned `RBVM_FORMULA_V1` artifact, not Priority or SLA.

## Repository hygiene

Generated Python bytecode and `__pycache__` directories are ignored, and the previously tracked generated bytecode artifact is removed.

This file is informational. The authoritative semantics remain the versioned contracts referenced by the README and the implementation/tests themselves.
